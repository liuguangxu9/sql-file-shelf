# SQL 文件架设计

## 范围

以 SQLShelf 的深色三栏视觉语言实现一个浏览器本地文件编辑器。第一版只处理 `.sql`、`.txt`、`.md`，支持选择工作目录、浏览、打开、编辑、直接保存和另存为。

## 权限与数据

用户通过 Edge 的目录选择器授权一个工作区。`FileSystemDirectoryHandle` 仅保存于该站点的 IndexedDB；每次启动检查读写授权。文件内容不会发往网络。最近打开项和主题偏好同样仅存在 IndexedDB。

## 文件保真

读取时保存原始字节、编码、BOM 和换行符。编码识别顺序为 BOM、严格 UTF-8、概率检测器。保存时先按原编码转回字节，再解码比较文本；出现不可表示字符则拒绝覆盖。直接保存会写入同目录 `.bak` 备份。

## UI

左栏为当前授权工作区和文件树，中栏为可多行编辑的文本区，右栏显示编码、BOM、换行符、置信度及保存操作。页面始终显示当前工作目录和权限状态。
