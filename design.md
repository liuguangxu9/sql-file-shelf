# SQL 文件架设计

## 信息架构

页面固定为亮色三栏：左侧 240px 管理多个工作区；中间 330px 以表格列出当前工作区内的文件名、修改时间和格式；右侧占据剩余宽度，作为编辑器。编码、BOM、换行符和保存操作均在编辑器顶部，因此文件内容始终拥有最大可用面积。

两条可拖动分隔线可调整三栏比例并持久化。工作区以拖放方式重排并带过渡动画；文件表格的文件名、修改时间、格式表头均可点击切换正序/倒序，截断路径用原生悬浮提示展示完整名称。编辑区包含同步滚动的行号和 SQL 关键字、字符串、注释、数字高亮层。

## 工作区与权限

每个用户选择的目录以 `FileSystemDirectoryHandle`、标识和名称保存至 IndexedDB。启动时只调用 `queryPermission()`，不会自动申请授权；无法访问的工作区保留在列表中，并提供单独的“恢复此目录访问”按钮。该按钮的首个特权调用是 `requestPermission()`，满足 Edge 的用户手势要求。

## 搜索与文件列表

左上搜索框跨所有已授权工作区检索文件名、相对路径和文本内容；命中项显示工作区和相对路径。中上搜索框只过滤当前工作区的文件表。文件扫描只处理 `.sql`、`.txt`、`.md`，并将结果按最后修改时间排序。

启动时，若恢复出的当前工作区仍具读写授权，立即重新扫描其文件；若授权已经失效，保留高亮工作区并提示用户恢复目录访问。文件栏提供手动“刷新”，确保外部修改能立即重新读取。

文件栏还提供根目录“＋ 新建”，仅允许 `.sql`、`.txt`、`.md` 文件名，且不会覆盖已有文件；已存在的同名文件只会在用户确认后打开。每个文件行的“×”会在明确确认后永久删除该文件，删除当前已打开文件时立即清空编辑器。

## 文件保真与离线更新

保存保留原编码、BOM 和换行符，写回前进行编码往返校验；直接保存生成 `.bak`。Service Worker 采用版本化缓存、激活时清理旧缓存，并使用网络优先策略，避免已安装或旧会话继续运行过期页面。
