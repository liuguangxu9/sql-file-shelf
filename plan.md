# 实施计划

1. 建立 File System Access 与编码保真核心，并对编码处理编写回归测试。
2. 将单目录状态重构为可持久化的工作区集合。
3. 建立独立文件模型，测试按修改时间排序和按文件名、路径、格式筛选。
4. 重做为亮色三栏：工作区、文件表格、宽编辑器。
5. 实现跨工作区全文搜索与当前工作区文件筛选。
6. 将权限恢复限制在用户点击事件内，并升级 Service Worker 缓存策略。
7. 构建、单元测试、浏览器 UI 验证，并发布 GitHub Pages。
8. 增加可持久化的三栏宽度拖拽、工作区拖放重排、文件列排序、行号与轻量 SQL 高亮。
9. 启动时刷新已授权的恢复工作区，并在文件栏增加手动刷新、新建根目录文件与二次确认删除文件功能。
