{
  "name": "sql-file-shelf",
  "version": "0.1.0",
  "private": true,
  "description": "Local-first SQL, text, and Markdown file editor for Edge",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "build": "node scripts/build.js",
    "serve": "npx http-server dist -p 4173 -c-1"
  },
  "dependencies": {
    "chardet": "^2.2.0",
    "iconv-lite": "^0.7.3"
  },
  "devDependencies": {
    "browserify": "^17.0.1",
    "http-server": "^14.1.1"
  }
}
