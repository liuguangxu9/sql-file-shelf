# Codex 工具映射

## 状态

待启用。SQL 文件架需要固定 HTTPS 地址作为 PWA origin，才能稳定保存 Edge 的目录句柄和安装状态；不能以 `file://` 双击方式替代。

## 启用条件

1. 将 `dist/` 发布到用户指定的 HTTPS 静态站点。
2. 在 Edge 中首次打开、安装为应用，并选择工作区目录。
3. 再为 Codex 建立“打开 SQL 文件架”的个人工具映射，指向该 HTTPS 地址。

在用户指定发布地址前，不创建会指向临时 `localhost` 的映射，以免工具失效或权限状态不稳定。
